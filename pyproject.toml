[project]
name = "pybadges"
version = "0.1.0"
description = ""
readme = "README.md"
authors = [
    { name = "toulibre", email = "contact@toulibre.org" }
]
requires-python = ">=3.8"

dependencies = [
    "pillow>=10.1.0,<11.0.0",
    "jsonschema>=4.23.0,<5.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
